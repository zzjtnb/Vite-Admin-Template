import { createStore } from 'vuex'
import menu from './modules/menu'//引入store(仓库)token(初始state对象)
export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    menu
  }
})
